/**
 * Feature modules are organised by domain.
 * Each feature owns its own: api/, components/, hooks/, types/, schemas/.
 *
 * Planned features:
 *   auth/          — login, register, session
 *   dashboard/     — user & admin dashboards
 *   committees/    — committee list & detail
 *   members/       — member management
 *   contributions/ — contribution tracking
 *   payments/      — payment history & verification
 *   cycles/        — cycle management
 *   lottery/       — lottery results
 *   payouts/       — payout tracking
 *   notifications/ — notification centre
 *   profile/       — user profile
 *   admin/         — admin-specific views
 *   ai-assistant/  — AI Committee Assistant
 */
